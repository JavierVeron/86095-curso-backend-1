/* Definiremos la función “mostrarLista”, la cual recibirá un arreglo (un array) con elementos como parámetro.

Si la lista está vacía, devolver un mensaje indicando “Lista vacía”.
Si la lista cuenta con elementos, mostrarlos 1 por 1 en consola. Finalizar el proceso devolviendo la longitud de la lista (Utilizar template strings)
Invocar la función con los casos de prueba.

Observaciones:
- crear un función que reciba como parámetro un array
- utlizar for, for..of, forEach
- utilizar console
- utilizar if
- utilizar la propiedad de array que se llama "length" (longitud de elementos)
- utlizar o no template strings (plantilla literal) */

const alumnos = ["Agustín", "Ambar", "Blas", "Cielo", "Francisco", "Santiago", "Nicolas", "Fernanda"];
const alumnos2 = [];

function mostrarLista(unArray) {
    //...
}

mostrarLista(alumnos);